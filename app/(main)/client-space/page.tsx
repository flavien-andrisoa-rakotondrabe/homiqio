import PlaceholderPage from '../../components/PlaceholderPage';

export default function ClientSpacePage() {
  return (
    <PlaceholderPage
      title="Espace client"
      description="Accueil de l'espace client (tableau de bord, liens vers profil, réservations, etc.). À personnaliser."
    />
  );
}
