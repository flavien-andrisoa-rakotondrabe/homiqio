import PlaceholderPage from '../../components/PlaceholderPage';

export default function LoginPage() {
  return (
    <PlaceholderPage
      title="Connexion"
      description="Page de connexion. À personnaliser (formulaire email/mot de passe, etc.)."
    />
  );
}
